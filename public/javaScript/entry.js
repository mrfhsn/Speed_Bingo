const entry = document.getElementById('entry-name-box')
const entry_button = document.getElementById('join-from-entry')

window.onload = () => {
    entry.focus()
}


entry_button.addEventListener("click", () => {})

entry.addEventListener("keypress", function(keyValue) {
    if(keyValue.key === "Enter") () => {}
})